plugins {
    id "loom"
}

version = mod_version

dependencies {
    it.minecraft "com.mojang:minecraft:$minecraft_version"
    mappings "net.fabricmc:yarn:$yarn_mappings:v2"
    subprojects.each { include it }
}

afterEvaluate {
    genSourcesWithVineflower {
        enabled = false
    }
}
